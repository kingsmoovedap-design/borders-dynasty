export const metadata = {
    name: "borders",
    version: "1.0.0",
    description: "Borders Dynasty plugin"
};

export function bordersCommands(args, runtime) {
    runtime.logger.info("Borders Dynasty plugin activated.");
}
